using System;
using System.IO;
using System.Threading.Tasks;

namespace SerializationTest
{
    class Program
    {
        private static async Task Main()
        {
//
//             await using (var stream = new MemoryStream())
//             {
//                 await serializer.Serialize(rootNode, stream);
// //                await using var anotherStream = new MemoryStream();
// //              var bf = new BinaryFormatter();
//   //              bf.Serialize(anotherStream, new [] {"lalala","lala"});
//                 var deserializedTree = await serializer.Deserialize(stream);
//             }
//
//             var nodeCopy = await serializer.DeepCopy(rootNode);
        }
    }
}